<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title><%= user.username %>'s awesome blog</title>
  <link href="https://fonts.googleapis.com/css?family=Share+Tech+Mono|Source+Sans+Pro:300,400,600,700" rel="stylesheet">
  <link rel="stylesheet" href="../master.css">
</head>
<body>
  <% include partials/header %>
  <div class="body-container">
    <h2><%= user.username %></h2>
    <p><%= user.bio %></p>
    <p># of posts: <%= user.blogPosts.length %></p>
    <div class="post-previews-container">
      <% for (var i = 0; i < user.blogPosts.length; i++) { %>
        <a href="/blogs/<%= user.username %>/<%= user.blogPosts[i]._id %>" class="post-preview-link">
          <h4><%= user.blogPosts[i].title %></h4>
          <p class="post-timestamp">
            Posted <span class="date-time"><%= user.blogPosts[i].posted %></span> |
            <% if (user.blogPosts[i].allowComments == true) { %>
              <%= user.blogPosts[i].comments.length %> Comments
            <% } else { %>
              Comments disabled
            <% } %>
          </p>
          <p class="post-body-preview"><%= user.blogPosts[i].body.substr(0,5) %>...</p>
          <p>View full post</p>
        </a>
        <% } %>
    </div>
  </div>
  <script type="text/javascript" src="../main.js"></script>
</body>
</html>
