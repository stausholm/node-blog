<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>homepage</title>
  <link href="https://fonts.googleapis.com/css?family=Share+Tech+Mono|Source+Sans+Pro:300,400,600,700" rel="stylesheet">
  <link rel="stylesheet" href="master.css">
</head>
<body>
  <% include partials/header %>
  <div class="body-container">
    <h1>Test homepage</h1>
    <% if (!user) { %>
      <a href="/auth/register">Sign up Today!</a>
    <% } %>
    <p>Check out these cool bloggers!</p>
    <div class="bloggers-container">
      <% for (var i = 0; i < data.length; i++) { %>
        <a href="/blogs/<%= data[i].username %>">
          <p>User: <%= data[i].username %></p>
          <p>Posts: <%= data[i].blogPosts.length %></p>
          <p>Bio: <%= data[i].bio %></p>
        </a>
        <% } %>
    </div>
  </div>
</body>
</html>
